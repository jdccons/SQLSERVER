SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO

CREATE FUNCTION [dbo].[udf_CalcRel] (@DOB DATETIME)
RETURNS NCHAR(1)
AS

BEGIN
	DECLARE @Age INTEGER
	SET @Age = dbo.fAgeCalc(@DOB)
	RETURN (
	SELECT CASE 
		WHEN @Age >= 18 THEN 'S'
		ELSE 'C'
	END		
	)
END

GO
